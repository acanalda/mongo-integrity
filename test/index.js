var assert = require('assert'),
  pingit = require('../index');

describe('#mongo-integrity', function() {

  it('nothing', function(done) {
    //assert.equal(miliseconds>0, true);

    done();
  });

});
